[aliases]
test=pytest

[tool:pytest]
addopts = --junitxml=build/reports/tests/junit.xml --cov=publish --cov-report html --cov-report term --cov-report xml

[coverage:html]
directory=build/reports/coverage

[coverage:xml]
output=build/reports/coverage.xml
